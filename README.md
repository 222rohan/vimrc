### Rohan's Vim Config

A very minimal to speed up basic usage of vim. Has some useful shortcuts to comment lines, manage buffers, and features like syntax highlighting, etc.
To use, copy the file contents from `vimrc` to `~/.vimrc`. It's divided into sections for ease of future modification. 
